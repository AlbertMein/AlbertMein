Review the current changes for code quality, security, and correctness.

1. Run `git diff` to see all staged and unstaged changes
2. For each changed file, apply the code-review skill checklist
3. Check for: hardcoded secrets, missing type hints, untested logic, security issues
4. Output a structured review with sections: ✅ Looks Good, ⚠️ Suggestions, 🚫 Must Fix
