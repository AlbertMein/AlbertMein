Run the project's test suite and analyze results.

1. Look for pytest configuration (pyproject.toml, pytest.ini, setup.cfg)
2. Run `python -m pytest -v --tb=short` (or the project's configured test command)
3. If tests fail, analyze failures and suggest fixes
4. Report: total tests, passed, failed, and any warnings
